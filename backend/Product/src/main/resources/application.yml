server:
  address: i4d106.p.ssafy.io
  port: 8076
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: wndeowkd
    url: jdbc:mysql://localhost:3306/product?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: eureka
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
